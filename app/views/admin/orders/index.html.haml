%h3 Orders
.row
  .span12
    %table.table.table-condensed
      %thead
        %tr
          %th Order Number
          %th Name
          %th Order Date
          %th Price
          %th
      %tbody
        - @orders.each do |order|
          %tr
            %td= order.id
            %td= order.name
            %td= order.created_at.strftime("%m/%d/%Y - %H:%M:%S")
            %td= order.complete_order_price(order.price_cents, order.price_currency)
            %td= link_to 'Order Details', admin_order_path(order), class: 'btn btn-mini'
