= simple_form_for([:admin, @buckle]) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :hex_color
    = f.input :image
    = f.association :style

  .form-actions
    = f.button :submit, class: 'btn btn-inverse'
